"""Adapters that run published reward models on WorldReward-Bench.

Each script emits a ``*.pairs.jsonl`` consumable by ``worldreward-bench/score.py``,
so every predictor in the Results table is scored by the same code.
"""
